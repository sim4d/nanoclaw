# Browser-Use Project

Browser automation toolkit for AI agents - Node.js implementation

## 📦 Installed Dependencies

- **Puppeteer** - Headless Chrome Node.js API
- **Playwright** - Browser automation framework
- Supporting libraries for browser control

## 🚀 Quick Start

### Basic Example

```javascript
const { BrowserAgent } = require('./browser-agent.js');

async function run() {
  const agent = new BrowserAgent();
  try {
    await agent.init();
    await agent.goto('https://example.com');
    const title = await agent.getTitle();
    console.log(title);
  } finally {
    await agent.close();
  }
}

run();
```

### Run Examples

```bash
# Example 1: Basic navigation
node browser-agent.js 1

# Example 2: Search functionality
node browser-agent.js 2

# Example 3: Extract links from Hacker News
node browser-agent.js 3
```

## 📋 Available Methods

| Method | Description |
|--------|-------------|
| `init()` | Initialize browser |
| `goto(url)` | Navigate to URL |
| `getTitle()` | Get page title |
| `getContent()` | Get page text content |
| `screenshot(filename)` | Take screenshot |
| `click(selector)` | Click element |
| `type(selector, text)` | Type text into input |
| `waitForSelector(selector)` | Wait for element |
| `getLinks()` | Extract all links |
| `evaluate(script)` | Execute custom JavaScript |
| `close()` | Close browser |

## 🎯 Common Use Cases

### Web Scraping
```javascript
await agent.goto('https://example.com');
const content = await agent.getContent();
```

### Form Filling
```javascript
await agent.goto('https://example.com/form');
await agent.type('#name', 'John Doe');
await agent.type('#email', 'john@example.com');
await agent.click('#submit');
```

### Screenshot Capture
```javascript
await agent.goto('https://example.com');
await agent.screenshot('page.png');
```

### Data Extraction
```javascript
await agent.goto('https://news.ycombinator.com');
const links = await agent.getLinks();
console.log(links);
```

## 📁 Project Structure

```
/workspace/group/browser-use-project/
├── browser-agent.js      # Main agent class
├── package.json          # Dependencies
├── node_modules/         # Installed packages
└── README.md            # This file
```

## 🔧 Configuration

The browser launches with these default options:
- Headless mode
- No sandbox (for containerized environments)
- Shared memory disabled
- GPU acceleration disabled

## 📝 Notes

- Screenshots are saved to `/workspace/group/browser-use-project/`
- Browser closes automatically when the agent is closed
- Error handling is built into all methods

## 🚨 Troubleshooting

If you encounter permission issues, the agent runs in a sandboxed environment with appropriate flags.

---

*Generated by Andy - AI Assistant*
